name: Filter Norway files

on:
  workflow_call:

jobs:
  upload-release-asset:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

  

      - name: APT Update
        run: sudo apt update  

      - name: Install OSM tools
        run: sudo apt install osmium-tool osmctools

      - name: Install rust
        run: rustup toolchain install stable

      - name: "Download Spain"
        uses: wei/wget@v1
        with:
          args: https://download.geofabrik.de/europe/spain-latest.osm.pbf

      - name: Filter spain for Pfaedle
        run: osmium tags-filter -o pfaedle-filtered-spain-latest.osm.pbf --expressions=pfaedle-filter-all.txt spain-latest.osm.pbf
  
      - name: "Upload spain filtered"
        env:
          GITHUB_TOKEN: ${{ secrets.GITUHB_TOKEN }}
          GH_TOKEN: ${{ github.token }}
          ASSET_PATH: pfaedle-filtered-spain-latest.osm.pbf
          ASSET_NAME: pfaedle-filtered-spain-latest.osm.pbf
          RELEASE_TAG: latest
        run: |
          echo "Uploading asset: $ASSET_PATH as $ASSET_NAME to release latest"
          gh release upload latest "$ASSET_PATH" --clobber
        shell: bash
        if: env.ASSET_PATH != '' && env.ASSET_NAME != ''
