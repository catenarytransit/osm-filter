name: Manual Update Trigger

on:
  workflow_dispatch:
    
permissions:
  contents: write
  packages: write

jobs:
  call-update:
    uses: ./.github/workflows/filter.yml
  call-update-norway:
    uses: ./.github/workflows/filter_norway.yml